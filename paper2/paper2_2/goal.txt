🧭 MỤC TIÊU TỔNG HỢP – H1 + H3 TRÊN NỀN DUAL-CORE

Trục chính hiện tại:
- Dual-core + ASCON DMA coherency đã đóng functional closure.
- `test_dualcore_ascon_dma_coherent` đã PASS ở mức SoC dual-core.
- `paper2/paper2_2` giờ là baseline ổn định để chốt H3 trước, rồi mở H1.

Ưu tiên ngắn hạn:
1. Chốt H3:
   - `context_id`
   - `CONTEXT_SEL` / CRF
   - firmware dual-core với 2 context độc lập
   - context-switch benchmark
2. Mở H1:
   - `crypto_pending`
   - queue/batching trong DCache
   - prototype ASCON datapath trong cache controller
   - test plaintext -> cache -> ciphertext
3. Làm rõ phần discussion:
   - baseline coherent DMA vs H3 vs H1
   - contention CPU/DMA, throughput, hit/miss tables

Ưu tiên trung hạn:
1. H3 - Multi-Context ASCON
   - thêm `context_id`
   - thêm `CONTEXT_SEL` / CRF
   - firmware dual-core với 2 context độc lập
2. H1 - Compute-in-Cache
   - thêm `crypto_pending`
   - queue/batching trong DCache
   - prototype ASCON datapath trong cache controller

File trọng tâm:
- `paper2/paper2_2/H1+H3.md`
- `paper2/paper2_2/rtl_coherency_checklist.md`
- `paper2/paper2_2/rtl_coherency_audit.md`
- `paper2/paper2_2/dualcore_missing_items.md`
- `paper2/paper2_2/dualcore_test_results.md`

Mốc trình bày:
- Paper 2: dual-core selective coherent DMA
- H3: hardware multi-context ASCON
- H1: compute-in-cache crypto processing
