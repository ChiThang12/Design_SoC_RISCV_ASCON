Generated flat RTL package for FPGA bring-up.

Do not use Verilog include-flow in this package. Compile the RTL files through:

```text
../script/rtl_sources.f
```

For synthesis, set the top module to:

```text
fpga_top
```
